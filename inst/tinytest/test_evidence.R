# cp1  <- compile(cpt_list(asia2), initialize_cpts = FALSE)
# cp1_ <- set_evidence(cp, evidence = c(bronc = "yes", either = "no", lung = "yes"), initialize_cpts = TRUE)

# j1   <- jt(cp1_)
# j1$charge


# cp2  <- compile(cpt_list(asia2))
# j2   <- jt(cp2, evidence = c(bronc = "yes", either = "no", lung = "yes"))
# j2$charge

# sparta_empty_unity <- function() sparta::sparta_unity_struct(list(EMPTYSET__ = character(0)), 1)
# sparta::sparta_unity_struct(list(character(0)), 1) |> unclass()
# emp <- sparta_empty_unity()
